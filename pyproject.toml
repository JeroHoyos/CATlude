[project]
name = "catlude"
version = "0.1.0"
description = "Tiny coding cat agent — like Claude Code but with a cat"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]

[project.scripts]
catlude = "catlude.agent:main"
